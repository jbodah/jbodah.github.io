version: '2'
services:
  blog:
    image: ruby
    volumes:
      - .:/opt/blog
    working_dir: /opt/blog
    ports:
      - 80:4000
    entrypoint: sh docker-entrypoint.sh
    command: bundle exec jekyll serve -H 0.0.0.0
